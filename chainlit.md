# AIหน้าฮ่าน 3D Digital Twin

**น้องหลงทาง** คือ Smart Mall Agentic Navigator สำหรับช่วยค้นหาสินค้าในห้าง เช็กสต็อก บอกตำแหน่งชั้นวาง แสดงตำแหน่งบนแผนที่ 3D และจัดเส้นทางเดินซื้อของให้ครับ

ระบบนี้ค้นจากฐานข้อมูลสินค้าจริงของโปรเจกต์ โดยใช้ Semantic Search, PostgreSQL + pgvector และ AI Agent workflow ผ่าน LangGraph / LangChain

## เริ่มใช้งาน

พิมพ์สิ่งที่อยากหาเป็นภาษาปกติได้เลย เช่น ชื่อสินค้า หมวดสินค้า งบประมาณ จำนวนรายการ หรือเงื่อนไขสต็อก

ตัวอย่างคำถาม:

- หาแชมพูไม่เกิน 200
- อยากได้ขนมมันฝรั่งทอด แนะนำ 3 แบรนด์
- มียาแก้ปวดอยู่ตรงไหน
- หาเครื่องใช้ไฟฟ้าราคาไม่เกิน 500
- มีสินค้าอะไรที่สต็อกเหลือ 0 บ้าง
- แนะนำเสื้อผ้าผู้ชาย 2 รายการ

## หมวดสินค้าที่รองรับ

น้องหลงทางดูแลสินค้า 5 หมวดนี้:

- อาหาร
- ของใช้ทั่วไป
- เสื้อผ้า
- เครื่องใช้ไฟฟ้า
- ยาสามัญ

ถ้าคำถามอยู่นอกขอบเขตนี้ ระบบจะตอบตรง ๆ ว่าอยู่นอกหน้าที่ แทนการเดาข้อมูลขึ้นมาเอง

## อ่านผลลัพธ์

คำตอบจะแสดงผลเป็นตารางสินค้า โดยมีข้อมูลหลัก:

- `ID` สำหรับใช้อ้างอิงหรือเพิ่มสินค้าเข้าลิสต์
- ชื่อสินค้าและแบรนด์
- หมวดสินค้า
- ราคา
- จำนวนคงเหลือ
- ตำแหน่งในห้าง

ตัวอย่างตำแหน่ง:

```text
โซน A ล็อค 1 ฝั่งซ้ายมือ ระดับสายตา
```

ให้เดินไปที่โซนและล็อคที่ระบุ แล้วดูฝั่งชั้นวางกับระดับชั้นตามที่ระบบบอก

## เปิดแผนที่ 3D

หลังจากค้นสินค้าแล้ว สามารถเปิดแผนที่ 3D เพื่อดูตำแหน่งสินค้าได้ที่:

[เปิด 3D Navigator](/public/model_viewer.html)

หรือพิมพ์ในแชต:

```text
แสดงแผนที่
```

ในหน้า 3D Viewer สามารถ:

- หมุนมุมมองด้วยการลากเมาส์ซ้าย
- ซูมด้วย scroll
- เลื่อนมุมมองด้วยการลากเมาส์ขวา
- ดู pin สินค้าที่ Agent ค้นเจอ
- ดูเส้นทางจากจุดเริ่มต้นไปยังตำแหน่งสินค้า

## เพิ่มสินค้าเข้าลิสต์

หลังจากค้นสินค้าแล้ว ให้ใช้ `ID` จากตารางเพื่อเพิ่มสินค้าเข้าลิสต์

เพิ่มรายการเดียว:

```text
เพิ่ม 12
```

เพิ่มหลายรายการ:

```text
เพิ่ม 12 34 56
```

เพิ่มแล้วเปิดแผนที่:

```text
เพิ่ม 12 แล้วแสดงแผนที่
```

เพิ่มจากลำดับในผลลัพธ์:

```text
เพิ่มรายการแรก
```

หรือ:

```text
เพิ่มทั้งหมด
```

## สรุปเส้นทางเดินซื้อของ

เมื่อเลือกสินค้าเสร็จแล้ว พิมพ์:

```text
สรุปเส้นทาง
```

น้องหลงทางจะจัดลำดับเส้นทางตามโซน A ถึง E พร้อมสรุปรายการสินค้าและราคารวม

## ตรวจสอบ Agent Logs

สามารถดูขั้นตอนการทำงานของ Agent ได้ที่:

[เปิด Agent Logs](/public/agent_logs.html)

หน้า Agent Logs ใช้ตรวจสอบ:

- `run_id` ของแต่ละคำถาม
- ขั้นตอนการทำงานของ workflow
- route ที่ Agent เลือก เช่น `search`, `cart`, `general`, `direct`
- constraints ที่ตีความจากคำถาม เช่น หมวด ราคา สต็อก จำนวนรายการ
- tool หรือ function ที่ถูกเรียก เช่น `search_products`, `fetch_products_by_ids`, `navigation_stock_node`
- จำนวนผลลัพธ์ที่ค้นเจอและผลลัพธ์ที่ผ่านการกรอง
- error หรือ warning ถ้ามี

หมายเหตุ: Log แสดง operational trace และ decision summary ของระบบ แต่ไม่แสดง hidden reasoning token ภายในโมเดล

## ข้อจำกัดของระบบ

- ค้นหาเฉพาะสินค้าในฐานข้อมูลของโปรเจกต์นี้
- ไม่เดาชื่อสินค้า ถ้าไม่พบหรือหมดสต็อกจะบอกตรง ๆ
- จัดเส้นทางจากตำแหน่งสินค้าที่มีข้อมูล location เท่านั้น
- ใช้ `ID` จากตารางสินค้าเมื่อต้องการเพิ่มเข้าลิสต์
- คำถามที่ไม่เกี่ยวกับการหาสินค้าในห้างจะถูกปฏิเสธอย่างสุภาพ

## เทคโนโลยีหลัก

- Chainlit สำหรับ Chat UI
- LangGraph และ LangChain สำหรับ Agent workflow
- DeepSeek ผ่าน OpenAI-compatible SDK สำหรับ LLM
- SentenceTransformers รุ่น `intfloat/multilingual-e5-large` สำหรับ embedding
- PostgreSQL + pgvector สำหรับ vector search
- Three.js สำหรับ 3D Navigator
